{
  "name": "hoahx-artifacts",
  "private": true,
  "version": "1.1.0",
  "description": "Published HOAhx go-live artifacts (the owners' Decision Register), deployed via Netlify.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:register": "node scripts/build-register.mjs",
    "pull:register": "node scripts/pull-register.mjs",
    "check:register": "node scripts/build-register.mjs --check"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
